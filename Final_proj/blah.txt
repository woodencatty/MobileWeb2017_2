app.js에 route 추가(8~10, 27~29)

www에 Device Socket 구성(마지막부분)

각 페이지 mysql client

ejs에 table 형태로 db의 내용을 표시(서프 기기관리 페이지 참조)
검색된 기기 리스트 -> 기기 추가(등록) -> DB에 내용 + 유저정보 가져가기

기기에 조작 api 넣을까? -> 가능은 하다.

-- 아이템 볼륨이 작음.
    -> 외부에서도 제어가 가능함을 어필. (IP를 이용하여 접속->웹페이지 조작)
    -> (?)General API제공 -> on/off/status
        -> 별도 페이지 필요(환자 상세페이지처럼?)

    ->Testbed기기(라즈베리파이)의 기능 추가
        -> LED(많이! 1단계 2단계 3단계 색깔)
        -> JSON형태로 패키지 내용 구성? 
        -> 가능은 한데..이거 되려나? http.request에 들어가는 json객체에 header형태로 추가
        
